/**
 * Modelo de configuração da chave da API do OpenWeather (ClimaMonitor).
 *
 * Copie este arquivo para `JS/configuracao-api.js` (já está no `.gitignore`, nunca é
 * versionado) e cole sua própria chave abaixo. O projeto não pode usar Node.js (restrição
 * do professor da disciplina), então não há backend para guardar a chave nem
 * build/bundler para injetá-la em tempo de compilação — o front-end chama a OpenWeather
 * direto do navegador com a chave local de cada pessoa. Decisão registrada em
 * `ideacao.md §14`.
 */

export const OPENWEATHER_API_KEY = 'COLE_SUA_CHAVE_AQUI';
