/**
 * Chave da API do OpenWeather (ClimaMonitor) — arquivo local, no `.gitignore`, nunca
 * versionado (o GitHub bloqueia push com segredo detectado). Veja
 * `JS/configuracao-api.example.js` para o modelo que é versionado.
 */

export const OPENWEATHER_API_KEY = 'f596c5fa0d9985209d853d42d8c8ccc7';
